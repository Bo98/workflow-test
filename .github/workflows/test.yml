name: CI

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    container: ghcr.io/homebrew/ubuntu16.04:master
    steps:
      - run: |
          echo "::warning::Hello there" >&2
